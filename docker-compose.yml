services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ciglspc:latest
    devices:
      - nvidia.com/gpu=all
    volumes:
      - ./:/app
    entrypoint: [ "/bin/sh", "-c", "exec /opt/venv/bin/python main.py ${HYDRA_ARGS}" ]
    stdin_open: true
    tty: true
