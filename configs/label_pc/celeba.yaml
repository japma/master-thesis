# LabelPC over CelebA's 40 binary attributes.

type: label_pc

dataset:
  name: celeba
  num_classes: 40

model:
  num_attributes: 40
  num_input_distributions: 10
  num_sums: 10
  num_repetitions: 5

training:
  epochs: 120
  learning_rate: 0.001
  batch_size: 256
