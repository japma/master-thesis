defaults:
  - autoencoder: default
  - cspn: default
  - inference: default
  - dataset: mnist
  - training: default
  - _self_

mode: train_cspn

seed: null

wandb_mode: online

# TODO update paths
paths:
  ae_checkpoint: null
  cspn_checkpoint: null
